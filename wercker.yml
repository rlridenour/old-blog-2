box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: academic

deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - leipert/git-push:
            gh_oauth: GIT_TOKEN
            repo: rlridenour/rlridenour.github.io
            branch: master
            basedir: public