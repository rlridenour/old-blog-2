box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: academic

deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - lukevivier/gh-pages:
            gh_oauth: GIT_TOKEN
            repo: rlridenour/rlridenour.github.io
			domain: randyridenour.net
            basedir: public