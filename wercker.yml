box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: academic

deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - gh-pages:
            token: GIT_TOKEN
            repo: rlridenour/rlridenour.github.io
            basedir: public